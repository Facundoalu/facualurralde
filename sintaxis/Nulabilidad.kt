package com.alumaster.androidalu

fun main () {
    var name:String? = "facu"
        println(name?.get(3) ?: "Flaco estas en cualquiera")                //!! (no va a ser null)
                                                                            //? (Si name no es null, hace algo
}
